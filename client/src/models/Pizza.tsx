export interface Pizza {
    size: string;
    sizePrice: number;
    crust: string;
    crustPrice: number;
    toppings: string[];
    toppingsPrice: number;
    maxToppings: number;
    totalPrice: number;
}
